# Task Archive

Task archives created by `ags archive` or `scripts/stop-archive-hook.sh` are
stored here in the user's local memory directory.

This template directory is intentionally empty in the public distribution. It
documents the expected structure without shipping any private task history.
