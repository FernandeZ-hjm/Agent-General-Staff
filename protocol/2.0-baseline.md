# AGS 2.0 — M0 Baseline Freeze

冻结日期：2026-06-05
冻结定位：M0 基线来源为 `ca7540dd0347433cc9cf71d2f14951ef74bcb2ea`；本文件与
M1 CLI 对象命令改动在 `ce92dc59ff6d20c1dfbb9052732f3d6db81c42c7` 同一 commit 落盘。

## 摘要

Agent Governance Suite 2.0 M0 基线冻结记录当前 `ags` CLI 命令面、协议边界、测试门禁、
private/stable/public-core-only 边界和 crate 结构。后续 M1-M8 以此基线为出发点，以
可验证的方式增量演进。

2.0 继续保持 public/core-only payload 边界：Rust `ags` 工具链、`crates/`、`target/`、
`Cargo.toml`、`Cargo.lock` 不进入 public payload。

## 命令面（M0 冻结，M1 升级，M2 新增）

| M0 命令（冻结） | M1 命令（新增/主推） | M2 命令（新增） | 库 crate | 说明 |
|---|---|---|---|---|
| `ags task-card-validator <paths>` | `ags task validate <paths>` | — | `task-card-validator` | 任务卡格式与语义校验 |
| `ags resolve-policy <path> [--format] [--approve-writes]` | `ags policy resolve <path> [--format] [--approve-writes]` | — | `execution-policy` | 执行策略解析 |
| `ags workflow-sync-check [--source] [--targets] [--format]` | `ags sync check [--source] [--targets] [--format]` | — | `workflow-sync-check` | 协议漂移检查 |
| `ags suite-doctor [--format]` | `ags doctor [--format]` | — | `suite-doctor` | 套件健康诊断 |
| `ags bootstrap-dry-run [--format]` | `ags bootstrap --dry-run [--format]` | — | `bootstrap-dry-run` | 引导干运行模拟 |
| — | — | `ags session preflight --for <agent> [--target] [--format]` | `project-discovery` | 聚合 Agent 唤醒检查（kernel activation 入口） |
| `ags verify --scope local|full|release [--target] [--format]` | `ags-verify` | 结构化验证入口（CheckItem 模型，text/json 双格式） |

输出格式：除 `task-card-validator` / `task validate` 外，M0/M1 命令支持
`--format text|json`（clap value_parser 强校验），非法 `--format` 值由 clap
拒绝并 exit 2。

exit code 约定：`text` 和 `json` 格式 exit code 语义一致；0 = pass，1 = fail/validation failed，2 = 参数错误。

## 协议文件目录

```
protocol/
  agent-task-protocol.md   # 任务卡与 review 规则（canonical）
  task-card-template.md     # 固定任务卡骨架
  runtime-adapters.md       # 执行器/权限/review/resume 规则 + execution-policy resolver 协议
  task-routing.md           # light/medium/heavy 路由
  project-profile.md        # 项目画像 schema
  context-memory.md         # 上下文记忆协议
  cursor-skill-index.md     # 技能索引
```

根目录入口文档：
- `AGENTS.md` — agent 入口
- `CLAUDE.md` — 执行协议
- `WORKSPACE.md` — 仓库角色映射
- `AGENT_SUITE_PROTOCOL.md` — 套件协议概述

## Crate 结构与依赖

```
Cargo.toml (workspace)
crates/
  ags-cli/                   # 统一 CLI 入口 → depends on all below
  task-card-validator/        # 任务卡校验库（canonical format gate）
  execution-policy/           # 执行策略解析器（M1-M10 规则引擎）
  workflow-sync-check/        # 协议漂移检查器（多目标 Markdown 章节级比较）
  suite-doctor/               # 共享诊断类型 + 套件健康检查
  bootstrap-dry-run/          # 引导干运行模拟 → depends on suite-doctor
  project-discovery/          # 项目发现与 Agent 感知（detect + protocol status + agent instructions + session preflight）
  task-compiler/              # 任务卡编译器（灵活意图 → canonical task card skeleton）
  capability-registry/        # 能力注册表（只读发现）
  receipt/                    # Receipt 生成/验证/合规检查
  ags-verify/                 # 结构化验证核心（CheckItem 模型 + scoped verification）
```

## 仓库边界

| Code | Role | Path |
|---|---|---|
| A | Development private suite | `/Volumes/AI Project/agent-governance-suite-private` |
| A1 | Private bare repo | `/Users/hujiaming/git-remotes/agent-governance-suite-private.git` |
| S | Stable private suite | `/Volumes/AI Project/agent-governance-suite-stable` |
| B | Public worktree | `/Volumes/AI Project/ai-dev-env-bootstrap` |
| B1 | Public bare repo | `/Users/hujiaming/git-remotes/ai-dev-env-bootstrap.git` |

边界规则：
- A → A1 只通过 `scripts/push-a1.sh`
- A1 → S 只通过 fast-forward pull
- A 不得直接修改 S
- Private Rust tooling 不进入 public payload（B/B1）
- Public promotion 走 allowlist 和 release checklist

## 测试门禁

| 门禁 | 命令 | M0 状态 |
|---|---|---|
| 格式化 | `cargo fmt --check` | OK |
| 单元测试 | `RUSTFLAGS="-D warnings" cargo test` | 437 passed, 3 ignored (19 suites) |
| 发布构建 | `cargo build --release` | OK |
| 集成验证 | `bash scripts/verify.sh` | OK（含 drift check, fixture validation, resolve-policy smoke, resolver hardening smoke） |
| Fixture 校验 | `ags task-card-validator tests/fixtures/valid-*.md` | OK |

### 单元测试分布（M0 基线）

| Crate | 测试文件 | 测试数 |
|---|---|---|
| `task-card-validator` | `src/lib.rs` | 结构化校验、错误码、fixture 覆盖 |
| `execution-policy` | `src/lib.rs` | 策略解析 M1-M10 规则、downgrade、stop_before_launch |
| `workflow-sync-check` | `src/lib.rs`, `src/parser.rs`, 等 | 协议解析、漂移检测、allowlist、assertions |
| `suite-doctor` | `src/lib.rs`, `src/checks.rs` | 诊断类型、checks、render |
| `bootstrap-dry-run` | `src/lib.rs` | 引导模拟检查 |

## execution-policy resolver 规则（M1-M10）

| Rule | 说明 |
|---|---|
| M1-M3 | `ultracode` 仅 thinking intensity，不改变 permission mode、parallelism、launch args |
| M4 | Heavy 任务无 explicit write approval → downgrade 到 plan-only + confirmation gate |
| M5-M6 | read-only/plan-only 不产生 write-type launch args，strip active parallelism 和 headless |
| M7 | subagent/multi-session/agent-team 需要 Workflow authority，worktree 需要非 none authority |
| M8-M10 | 每次 downgrade 记录结构化 DowngradeReason，generic adapter 限制 permission 为 plan-only |

## 协议安全断言（workflow-sync-check 强制执行）

1. `ultracode thinking-only` — ultracode 不提升权限
2. `Heavy downgrade` — Heavy 无批准降级到 plan-only
3. `read-only/plan-only no-write` — 不产生写入类 launch args
4. `runner resolver-first` — runner 必须消费 resolved policy JSON，不从原始字段拼接参数

## 2.0 承诺

- public/core-only payload 边界不变
- `task-card-template.md` 固定骨架不变
- task-card-validator canonical gate 地位不变
- execution-policy resolver (M1-M10) 规则不变
- verify.sh 门禁不变（新增兼容性测试为 additive）
- 库 crate 的 pub API backward compatible（M1 CLI 仅改 ags-cli 命令面）
