[package]
name = "ags-mcp"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Thin AGS MCP adapter (contract v3): two tools — ags_decide / ags_apply — over stdio JSON-RPC, plus the ags-host lifecycle executable"

[[bin]]
name = "ags-mcp"
path = "src/main.rs"

[[bin]]
name = "ags-host"
path = "src/bin/ags-host.rs"

[dependencies]
ags-kernel.workspace = true
serde.workspace = true
serde_json.workspace = true

[dev-dependencies]
tempfile.workspace = true
