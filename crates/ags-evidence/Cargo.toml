[package]
name = "ags-evidence"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "AGS receipt — task run receipt generation, verification, and compliance checking"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ags-platform = { path = "../ags-platform" }
ags-governance-decision = { path = "../ags-governance-decision" }
ags-task-contract = { path = "../ags-task-contract" }

[dev-dependencies]
tempfile = "3"
