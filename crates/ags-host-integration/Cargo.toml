[package]
name = "ags-host-integration"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "AGS governed-host identity and native memory lifecycle integration"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ags-platform = { path = "../ags-platform" }

[dev-dependencies]
tempfile = "3"
