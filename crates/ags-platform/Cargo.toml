[package]
name = "ags-platform"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Cross-platform paths, atomic files, process lookup, and hashing for AGS"

[dependencies]
sha2 = "0.10"
tempfile = "3"
