[package]
name = "ags-onboarding"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Deterministic AGS public onboarding assessment, plans, and closed actions"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
sha2 = "0.10"
