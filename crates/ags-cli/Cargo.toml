[package]
name = "ags-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Thin AGS unified CLI (contract v3): init / run / apply / check / test / log / status / doctor / update / skill / route / govern / schema"

[[bin]]
name = "ags"
path = "src/main.rs"

[dependencies]
ags-kernel.workspace = true
ags-task-contract.workspace = true
clap.workspace = true
serde.workspace = true
serde_json.workspace = true
toml.workspace = true

[dev-dependencies]
tempfile.workspace = true
