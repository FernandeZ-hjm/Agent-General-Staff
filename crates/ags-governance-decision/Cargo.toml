[package]
name = "ags-governance-decision"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "AGS typed host proposal validation and decision-lease contracts"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ags-platform = { path = "../ags-platform" }

[dev-dependencies]
serde_json = "1"
